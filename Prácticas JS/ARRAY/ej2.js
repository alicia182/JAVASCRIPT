let array = []

for (let i = 0; i < 3; i++) { // es menor que 3 porque el 0 ya cuenta como una vez, por eso es menor y no igual o menor
    array.push = window.prompt('Introduce un algo: ')
    
}

console.log(array[1]) // el segundo indice
console.log(array[2])
console.log(array[0])

/* tambien puedes poner console.log(array[0], array[2], array[0])