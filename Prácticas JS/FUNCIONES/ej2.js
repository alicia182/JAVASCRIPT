/*Escribe una función que escriba “Hola” tantas veces como el número que le pasamos de parámetro. 
Llámala dos veces, primero con 5 y después con 3.*/


saluodo(5)
saluodo(3)

function saluodo(numero){
    for (let i = 0; i < numero; i++) { // aqui en numero iria el 5 y el 3
        const element = array[i];
        console.log
    }
}


/*function saludo(primerHola, segundoHola) {
    let resultado = primerHola + segundoHola;
    alert("El resultado es " + resultado);
    }
    // Declaración de las variables
    let hola1 = 5;
    let hola2 = 3;
    // Llamada a la función
    saludo(hola1, hola2);*/